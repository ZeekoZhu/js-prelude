import { nxDebugHasherRunner } from './nx-debug-hasher-runner';

describe('nxDebugHasherRunner', () => {
  it('should work', () => {
    expect(nxDebugHasherRunner()).toEqual('nx-debug-hasher-runner');
  });
});
